import React from 'react'

export default function UpdateWarehouseProductForm({ onClose, product }) {
    return (
        <div>update-warehouse-product-form for product ID: {product.id}</div>
    )
}
